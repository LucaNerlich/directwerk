/**
 * OpenAPI contract tower — generated TypeScript types from the exported backend spec.
 *
 * Regenerate after API changes:
 *   cd Directwerk && ./gradlew :directwerk-app:exportOpenApi
 *   pnpm --filter @directwerk/api generate:openapi
 *
 * Hand-written runtime parsers in src/validation/ migrate incrementally to these types.
 * See CONTEXT.md #2 and migration order item 10.
 */
export type paths = Record<string, never>
