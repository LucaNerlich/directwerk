'use client'

/** Shared studio API transport — domain *Api.ts modules import from here. */
export {
    authenticatedRequest,
    jsonInit,
    postJson,
    proxyRequest,
    request,
} from './transport'
