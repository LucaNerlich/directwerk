'use client'

/** Shared studio API transport — domain *Api.ts modules import from here. */
export {
    authedFetch,
    authenticatedRequest,
    INVALID_RESPONSE,
    jsonInit,
    jsonRequest,
    postJson,
    proxyRequest,
    request,
} from './transport'
