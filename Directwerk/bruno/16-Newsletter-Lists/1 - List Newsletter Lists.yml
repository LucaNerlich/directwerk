info:
  name: 1 - List Newsletter Lists
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/newsletter-lists"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
