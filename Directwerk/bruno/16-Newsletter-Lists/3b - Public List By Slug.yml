info:
  name: 3b - Public List By Slug
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/newsletter-lists/weekly"
  headers:
    - name: Host
      value: "{{tenantAHost}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
