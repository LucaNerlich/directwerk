info:
  name: 3a - Public List Catalog
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/newsletter-lists"
  headers:
    - name: Host
      value: "{{tenantAHost}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
