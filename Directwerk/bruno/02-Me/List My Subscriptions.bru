meta {
  name: List My Subscriptions
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/me/subscriptions
  body: none
  auth: inherit
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200).to.equal(true);
  });
}
