info:
  name: Get My Access
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/access"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: "Entitlement summary: activeLevels, maxLevelSortOrder, activePackages, roles, tenantId."
