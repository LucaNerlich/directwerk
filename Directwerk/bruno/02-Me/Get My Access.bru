meta {
  name: Get My Access
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/me/access
  body: none
  auth: inherit
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200).to.equal(true);
  });
}

docs {
  Entitlement summary: activeLevels, maxLevelSortOrder, activePackages, roles, tenantId.
}
