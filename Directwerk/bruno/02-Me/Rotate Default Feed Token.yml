info:
  name: Rotate Default Feed Token
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/me/feeds/default/rotate-token"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: if (res.getStatus() === 200) { bru.setVar("subscriberFeedUrl", res.body.data.url); }
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200 || res.getStatus() === 403).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Invalidates the previous feed URL/token.
