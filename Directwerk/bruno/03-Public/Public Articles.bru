meta {
  name: Public Articles
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/public/articles
  body: none
  auth: none
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200).to.equal(true);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
