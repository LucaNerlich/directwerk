info:
  name: Site Config (Unknown Host)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/site-config"
  headers:
    - name: Host
      value: unknown-tenant.localhost

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 404).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
