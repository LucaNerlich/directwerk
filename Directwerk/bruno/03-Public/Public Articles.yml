info:
  name: Public Articles
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/articles"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Published articles for the tenant host. Each item may include `heroImageUrl`
  (public CDN) when a public hero asset is attached.
