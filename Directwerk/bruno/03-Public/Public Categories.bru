meta {
  name: Public Categories
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/public/categories
  body: none
  auth: none
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200).to.equal(true);
  });
}
