meta {
  name: Site Config (Tenant B)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/public/site-config
  body: none
  auth: none
}

headers {
  Host: {{tenantBHost}}
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200).to.equal(true);
  });
}
