meta {
  name: ALTCHA Challenge
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/public/altcha/challenge
  body: none
  auth: none
}

tests {
  test("returns challenge JSON", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.include("challenge");
  });
}
