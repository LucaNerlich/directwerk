info:
  name: Security Subscriber Probe
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/security/subscriber"
  auth:
    type: bearer
    token: "{{subscriberAccessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
