meta {
  name: 1 - List Module Definitions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/platform/modules
  body: none
  auth: inherit
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200).to.equal(true);
  });
}
