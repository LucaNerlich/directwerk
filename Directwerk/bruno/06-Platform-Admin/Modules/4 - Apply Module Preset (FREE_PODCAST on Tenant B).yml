info:
  name: 4 - Apply Module Preset (FREE_PODCAST on Tenant B)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/platform/tenants/{{tenantBId}}/modules/preset/FREE_PODCAST"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
