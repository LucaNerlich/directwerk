info:
  name: 6 - Reactivate Module (PODCAST on Tenant A)
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/v1/platform/tenants/{{tenantAId}}/modules/PODCAST/activate"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Restores PODCAST after request 5's deactivate demo -- 09-Podcast-Content and 02-Me's episode endpoints assume this is active.
