meta {
  name: 5 - Reactivate Tenant C
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/platform/tenants/{{tenantCId}}/reactivate
  body: none
  auth: inherit
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200 || res.getStatus() === 400 || res.getStatus() === 404).to.equal(true);
  });
}
