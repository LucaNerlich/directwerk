info:
  name: 6 - List Package Product Rules
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/tenant/products/{{packageProductId}}/rules"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200 || res.getStatus() === 404).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: 404 if Products/5 - Create Package Product above hit its known 500 case and packageProductId never got captured.
