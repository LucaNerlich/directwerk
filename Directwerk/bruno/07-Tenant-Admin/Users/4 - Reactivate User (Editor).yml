info:
  name: 4 - Reactivate User (Editor)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/tenant/users/{{editorUserId}}/reactivate"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200 || res.getStatus() === 404).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
