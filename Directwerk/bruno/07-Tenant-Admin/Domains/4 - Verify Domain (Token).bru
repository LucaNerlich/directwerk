meta { name: Verify Domain type: http seq: 4 }
post { url: {{baseUrl}}/api/v1/tenant/domains/{{newDomainHost}}/verify body: none auth: inherit }
tests { test("status", function() { expect(res.getStatus()===200||res.getStatus()===400).to.equal(true); }); }
docs { DNS TXT required; use platform force-verify locally without DNS. }
