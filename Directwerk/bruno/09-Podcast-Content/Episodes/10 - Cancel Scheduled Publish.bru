meta {
  name: 10 - Cancel Scheduled Publish
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/v1/episodes/{{podcastEpisodeId}}/cancel-schedule
  body: none
  auth: inherit
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200 || res.getStatus() === 400 || res.getStatus() === 404).to.equal(true);
  });
}
