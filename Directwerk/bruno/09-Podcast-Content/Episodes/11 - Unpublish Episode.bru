meta {
  name: 11 - Unpublish Episode
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/episodes/{{podcastEpisodeId}}/unpublish
  body: none
  auth: inherit
}

tests {
  test("returns expected status", function() {
    expect(res.getStatus() === 200 || res.getStatus() === 400 || res.getStatus() === 404).to.equal(true);
  });
}
