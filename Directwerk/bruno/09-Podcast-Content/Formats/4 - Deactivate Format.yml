info:
  name: 4 - Deactivate Format
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/formats/{{podcastFormatId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus() === 200).to.equal(true);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
