info:
  name: 08-Media
  type: folder
  seq: 8

request:
  headers:
    - name: Host
      value: "{{tenantAHost}}"
  auth:
    type: bearer
    token: "{{tenantAAccessToken}}"

docs:
  content: "Requires EDITOR or TENANT_ADMIN + DIGITAL_CONTENT module. Flow: upload-url -> client PUT to uploadUrl (outside Bruno) -> confirm -> preview-url. Without a real PUT, confirm returns 400 UPLOAD_VALIDATION_FAILED, which is expected."
  type: text/markdown
