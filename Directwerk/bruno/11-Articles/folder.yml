info:
  name: 11-Articles
  type: folder
  seq: 11

request:
  headers:
    - name: Host
      value: "{{tenantAHost}}"
  auth:
    type: bearer
    token: "{{editorAccessToken}}"

docs:
  content: |-
    Write desk (directwerk-newsletter) — full article draft → publish/schedule/unpublish/archive/unarchive workflow.
    Requires DIGITAL_CONTENT module and EDITOR or TENANT_ADMIN. Run 01-Auth-and-Tokens (editor login) first.
    Optional: 09-Podcast-Content/Categories for replace-categories requests.
    Run 6 before 9–10; run 10 before 11 (archive → unarchive).
  type: text/markdown
