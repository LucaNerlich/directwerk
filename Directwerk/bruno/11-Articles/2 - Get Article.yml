info:
  name: 2 - Get Article
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/articles/{{articleId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns expected status", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Requires a prior Create Article Draft run (captures articleId) or a known article id.
