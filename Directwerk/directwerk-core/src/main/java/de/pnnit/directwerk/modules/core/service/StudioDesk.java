package de.pnnit.directwerk.modules.core.service;

/**
 * Creator-facing desks in {@code directwerk-studio}. Nav visibility follows enabled modules.
 * Desks map to content channels: write (articles) vs podcast (episodes).
 */
public enum StudioDesk {
    WRITE,
    PODCAST
}
